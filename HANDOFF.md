# Handoff — Your Internet Radio Dial

> **Read these first (for the fresh-chat assistant):**
>
> 1. `.auto-memory/user_profile.md` — who David is and how to work with him
> 2. `.auto-memory/feedback_collaboration.md` — collaboration style (terse responses, milestone-based, patch diffs, etc.)
> 3. `.auto-memory/project_radio_dial.md` — project vision and constraints
> 4. `.auto-memory/project_hls_proxy_milestone.md` — the M2c proxy (architecture reference)
> 5. `.auto-memory/project_m3_milestone.md` — M3 library management (search + groups + drawer)
> 6. `.auto-memory/project_m4_milestone.md` — M4 Supabase auth + cloud sync
> 7. `.auto-memory/project_m5_polish_milestone.md` — M5 pre-deploy polish
> 8. `.auto-memory/project_m6_signin_milestone.md` — M6 magic-link sign-in
> 9. `.auto-memory/project_m7_rotate_overlay.md` — M7 rotate-to-landscape overlay
> 10. `.auto-memory/project_m8_url_add_milestone.md` — M8 add-station-by-URL
> 11. `.auto-memory/project_m9_curated_seed_milestone.md` — M9 curated default library
> 12. `.auto-memory/project_m10_reconnect_milestone.md` — M10 auto-reconnect on stream drops
> 13. `.auto-memory/project_m11_prewarm_milestone.md` — M11 dual-slot pre-warm + instant swap (eliminates the reconnect blip)
> 14. `.auto-memory/project_m12_polish_milestone.md` — M12 affordance polish + Suggestion Box
> 15. `.auto-memory/project_m13_features_milestone.md` — M13 Doze, Tone (Bass/Treble), Scan, Station Detail Card
> 16. `.auto-memory/project_m14_deploy_milestone.md` — M14 deploy + suggestion-box polish + email pipeline (live)
> 17. `.auto-memory/project_m15_cabinet_layout_milestone.md` — M15 cabinet layout rearrangement
> 18. `.auto-memory/project_m16_vertical_spacing_milestone.md` — M16 vertical spacing polish
> 19. `.auto-memory/project_m17_reduced_motion_caption_milestone.md` — M17 reduced-motion caption (drag-to-pan replaces marquee when prefers-reduced-motion is set)
> 20. `.auto-memory/project_m18_song_id_milestone.md` — M18 NOW PLAYING tap-to-identify song-ID lozenge (AudD + Supabase cache)
> 21. `.auto-memory/project_m19_tuning_static_milestone.md` — M19 between-station tuning static + reconnect bridge
> 22. `.auto-memory/project_m18_ios_postmortem.md` — three M18 follow-up commits + diagnosis of the iOS engine bug (read this before M20)
> 23. `.auto-memory/reference_ios_webaudio_silent_tap.md` — the iOS Safari MediaElementAudioSourceNode silent-tap bug; what's affected and the fix path
> 24. `.auto-memory/reference_radio_store_slices.md` — `s.playback.isPlaying` not `s.isPlaying` (slice-confusion trap)
> 25. `.auto-memory/project_deploy_checklist.md` — deploy punch list if that's where we are
>
> Then the rest of this doc. These memory files live at `<workspace>/.auto-memory/` (or at the user's global memory directory if the workspace copy isn't present — `MEMORY.md` lists them).

**Status (2026-04-30, late):** M1 → M20 shipped. M1 → M19 are **live in production** at https://yourinternetradiodial.net (Vercel auto-deploy from `main`, prod Supabase has the `song_id_cache` schema patch applied). Three additional M18 follow-up commits also live (see M18 iOS post-mortem below). M20 is built and ready to push.

**iOS limitation (now handled by M20):** On iPhone Safari/Chrome, `MediaElementAudioSourceNode` produces zero samples through the WebAudio graph even though `<audio>` plays normally to the speaker. The originally-proposed M20 fix (rewire to `el.captureStream() → createMediaStreamSource`) was diagnosed and ruled out: `captureStream` is not implemented on either `<audio>` or `<video>` HTMLMediaElements in iOS Safari. WebRTC loopback was considered but has no upstream MediaStream source on iOS. Custom JS streaming-decoder path was a 1-2 week rewrite — disproportionate. M20 ships **graceful iOS degradation** instead: VOLUME knob greyed out with "iPhone: use hardware buttons" caption (Apple silently ignores programmatic `el.volume` writes on `<audio>` as platform policy — only hardware buttons control output level); BASS/TREBLE greyed with persistent "iPhone: not available" caption (the `title`-attribute tooltip is invisible on iOS touch devices, so the explanation is inline always-on text); DOZE timer still fires (hard cut at end, no audible 30s fade); VU meter shows a permanent grey "VU meter not available on iPhone" overlay; NOW PLAYING lozenge stays visible but cream window says "Song identification not available on iPhone" with the tap disabled. Audio + tuning static + library + UI all work. M17.1 Suggestion Box RLS hotfix shipped in the M18 push. (Vercel auto-deploy from `main`). Suggestion Box submissions land in Supabase `public.suggestions` and email David at `dmf23@dawgranch.org` within seconds via Resend. Full library management — search radio-browser, add stations by URL, rename/reorder/delete bands, reorder/remove stations. Supabase auth + cloud sync complete (anon-by-default, email upgrade preserves uid + data, magic-link sign-in for cross-device library load). M5 cleared the pre-deploy UX backlog. M6 added cross-device sign-in (overwrite, not merge). M7 added a rotate-to-landscape nudge for phones in portrait. M8 added "Add by URL" path inside the search overlay. M9 replaced the seed with David's curated library (7 bands, 49 stations). M10 added transparent auto-reconnect when Vercel times out the proxy stream — retries 3x with backoff before showing SIGNAL LOST. M11 went one further: dual-slot pre-warm 30s before the expected cut, instant swap with no audible blip. M12 polished cabinet affordances and added the Supabase-backed Suggestion Box. M13 shipped Doze, Tone, Scan, and a Station Detail Card. M14 wrapped up deployment, fixed empty-band data corruption via `restoreEmptySeedBands`, polished the Suggestion Box (cream Contact Email field, magnifying-glass directory search w/ autopopulate), and wired the Resend → Edge Function → Database Webhook email-on-suggestion pipeline. M15 rearranged the cabinet — POWER button promoted to upper-left mirroring the "?", SUGGESTION BOX centered at the top, ON AIR lamp pulled up to sit midway between dial face and band ribbon, Model No. plate moved off the footer and into the dial face under "A Fruchtomania Production"; both upper cabinet rivets removed. M16 tightened vertical spacing across the cabinet — maker's plate margins reduced, VU/Volume column gap tightened, VOLUME label pulled into the empty space below the OFF/MAX ring, ToneKnob box height shrunk 92→80, service-controls strip switched to top-aligned so DOZE/DRIFT tops align with bass/treble knob tops. M17 fixed a "whip-fast caption scroll" bug on machines with `prefers-reduced-motion: reduce` — the global reduced-motion CSS rule was overriding the marquee's `animation-duration` to 0.01ms, which turned the *infinite* keyframe into a ~100 kHz strobe. ScrollingCaption now detects reduced-motion explicitly and swaps the marquee for a drag-to-pan single-copy view (5px threshold disambiguates click from drag, click still opens the StationDetailCard); home-machine behavior is unchanged. Bundled with a defensive `document.fonts.ready` re-measure for the latent font-swap-after-measure case. M18 added a **NOW PLAYING** tap-to-identify song-ID lozenge — brass plaque centered under the dial face that captures ~8s of the live audio output (via a parallel `MediaStreamAudioDestinationNode` tap on the analyser output) and POSTs it to a new `/api/song-id` route, which forwards to AudD with a 60s station-level Supabase cache (`song_id_cache` table, anon-permissive RLS). Tap model — not auto — keeps cost predictable; AudD $5/mo (5,000 IDs) is the entry tier. ON AIR lamp re-aligned to the bottom of its spacer to make room for the larger plate above. Also added a Suggestion Box RLS hotfix (M17.1) — that table's row inserts now always send `user_id: null` regardless of client auth state, sidestepping a class of RLS failures (stale session, expired JWT, iOS Safari ITP, etc). M19 added between-station tuning static — pink-noise+crackle generated in Web Audio (Kellet IIR cascade + 1-pole HP + sparse crackle), summed into the analyser input so it obeys EQ/master/Doze and drives VU. Every user-initiated tune (dial click, drum drag-release, band button, drawer click, search-add, scan tick, power-on) plays a 150ms-fade-in / 400ms-plateau / 300ms-lock-in envelope; reconnect attempts get the same envelope as a silent-gap bridge. M11 pre-warm swap stays silent. Five bugs caught + fixed during iteration are documented in the M19 milestone memory.

**Post-M17 hygiene:** Vercel Analytics installed (`@vercel/analytics`) and wired into `src/app/layout.tsx` as `<Analytics />` just before `</body>` — invisible to users, fires page-view events. `public/robots.txt` added with `User-agent: * / Disallow: /api/` so search engines skip the proxy routes (`/api/stream`, `/api/hls`, `/api/now-playing`, `/api/stations`, `/api/song-id`).

**Post-M18 polish (three follow-up commits, all live):**
1. Plate narrowed ~25% — mobile 340→260px, desktop 420→320px.
2. Top padding tightened (12→6px above "NOW PLAYING") and **marquee scroll** added to the cream window when body text overflows. Mirrors the DialWindow ScrollingCaption pattern (two copies + global `@keyframes marquee`, ~50px/sec, 8s minimum).
3. **Drag-to-pan** fallback for `prefers-reduced-motion: reduce` users — same `setPointerCapture` + 5px DRAG_THRESHOLD + `suppressClickRef` pattern as the dial caption. Drag handlers attach to the cream window (not the whole button), so a tap inside the cream window without drag still triggers song-ID. Final visual specs in `.auto-memory/project_m18_song_id_milestone.md`.

**Git state:** Working tree clean on `main`. Latest commits (most recent first):
- M18 fix: capture diagnostics + direct-destination ScriptProcessor connection (iOS investigation continues)
- M18 fix: capture WAV via PCM tap instead of MediaRecorder (iOS Safari was producing fragmented MP4 AudD couldn't decode)
- M18 fix: derive song-ID upload filename from blob MIME (iOS MP4 was being mislabelled .webm) + surface upstream error in lozenge
- M19: between-station tuning static + reconnect bridge
- M18 polish — drag-to-pan fallback for reduced-motion overflow text
- M18 polish — tighten top padding + marquee scroll for overflow
- M18 polish — narrow NOW PLAYING plate ~25%
- M18 — NOW PLAYING song-ID lozenge (AudD + cache + Console layout) + M17.1 Suggestion Box RLS hotfix
- robots.txt + Vercel Analytics + M17 (see prior entries)
- (older M-series tags above)

The three M18-fix commits at the top look like band-aids in retrospect — the diagnostics commit (#3) was the one that paid off because it let us read the failure conditions off the iPhone screen and identify the real cause as engine-level. None reverted; they're all real improvements (correct filename mapping, cleaner WAV-PCM capture path, better error surfacing) that will do their job once M20 lands real signal in the analyser tap.

Don't trust this list as the source of truth for hashes — David should `git log --oneline -10` from PowerShell if exact hashes matter.

Origin: https://github.com/dmf23-ai/your-internet-radio-dial.git. Vercel project linked, env vars set, Supabase URL configuration matches the live domain.

---

## What's built

| Milestone | Scope | Key files |
|-----------|-------|-----------|
| M1 | Seed data, dial UI, station caption lozenge | `src/data/seed.ts`, `src/components/Console.tsx`, `TunerKnob.tsx`, `DialWindow.tsx` |
| M2a | Audio engine (play/pause/volume/status), Zustand, IndexedDB (idb-keyval) | `src/lib/audio.ts`, `src/lib/store.ts`, `src/lib/storage.ts` |
| M2b | Now-playing metadata proxy + scrolling marquee caption | `src/app/api/now-playing/route.ts`, `DialWindow.tsx` |
| M2c | `/api/stream` (same-origin passthrough), `/api/hls` (manifest-rewriting), two-element audio engine, VU meter driven by real RMS, PresetBar wired to groups | `src/app/api/stream/route.ts`, `src/app/api/hls/route.ts`, `src/components/VUMeter.tsx`, `PresetBar.tsx` |
| M3 | `/api/stations` radio-browser proxy, search overlay (add), group editor (rename/reorder/create/delete), station-list drawer (tune/reorder/remove), dense-position invariant on all mutations | `src/app/api/stations/route.ts`, `src/components/SearchOverlay.tsx`, `GroupEditor.tsx`, `StationListDrawer.tsx`, `src/lib/store.ts` |
| M4 | Supabase schema + RLS, anonymous-by-default auth, write-through dual-sync (IndexedDB + cloud), pull-on-load, email upgrade that preserves uid & data, AccountDrawer + preset-bar person button, auth-change subscription (email confirmation flips Guest → Signed in without reload; sign-out mints fresh anon) | `supabase/schema.sql`, `src/lib/supabase/client.ts`, `src/lib/supabase/sync.ts`, `src/components/StoreHydrator.tsx`, `src/components/AccountDrawer.tsx`, `src/components/PresetBar.tsx` |
| M5 | Pre-deploy polish: drum-style dial ticker (station overflow), PresetBar edge-fade + drag-scroll + brass arrow-steppers (group overflow), "New Band" brass plaque, A FRUCHTOMANIA PRODUCTION tagline, vertical brass RADIO/BANDS labels, About/How-to overlay with "?" button, cathedral-radio favicon | `src/components/DialWindow.tsx`, `PresetBar.tsx`, `AboutOverlay.tsx`, `VerticalBrassLabel.tsx`, `src/app/icon.svg` |
| M6 | Cross-device magic-link sign-in. AccountDrawer's anon state has a "new account" / "sign in" mode toggle. Sign-in path uses `signInWithOtp({ shouldCreateUser:false })` so unknown emails get a clear error. StoreHydrator tracks the active uid in a ref and runs `pullFromCloud + applyCloudSnapshot` on auth-change when the uid actually changes (overwrite-not-merge semantics for the device's pre-sign-in guest library) | `src/lib/supabase/client.ts`, `src/components/AccountDrawer.tsx`, `src/components/StoreHydrator.tsx` |
| M7 | Mobile rotate-to-landscape nudge. Full-viewport overlay shows on phones held in portrait — animated phone-rotation icon + brass/walnut styling matching the cabinet. Detection: `(orientation: portrait) and (max-width: 820px) and (pointer: coarse)`. Dismissable per portrait session; resets on orientation change. (Browsers don't permit true orientation lock outside fullscreen, so this is the standard nudge pattern.) | `src/components/RotateOverlay.tsx`, `src/app/layout.tsx` |
| M8 | Add-station-by-URL. SearchOverlay grew a brass segmented "Search Directory / By URL" tab switcher. URL form: stream URL + station name, validates URL parses with http(s) protocol, infers `streamType` from path extension (.m3u8 → hls, .mp3, .aac, .ogg, else "unknown"), defaults `corsOk:false` so the proxy path is used. Reuses existing URL-based dedup in `addStationToGroup` | `src/components/SearchOverlay.tsx` |
| M9 | Curated default library. Replaced the bundled 4-band/22-station starter seed with David's personal export (7 bands, 49 stations: Favorites, Austin, Jazz, News/Talk, Ambient/Electronic, Around the World, Exploratorium). New users land on Favorites with WEFUNK preselected. Bumped `CURRENT_VERSION` 9→10 so existing devices invalidate their IndexedDB and re-seed on next load (signed-in users immediately re-pull from cloud) | `src/data/seed.ts`, `src/lib/storage.ts` |
| M10 | Auto-reconnect on stream drop. When Vercel terminates the `/api/stream` or `/api/hls` function mid-playback (or any other transient drop), the engine now retries up to 3x with [500ms, 1500ms, 4000ms] backoff. Lamp shows "Tuning…" during retries; SIGNAL LOST appears only after exhausting retries. New engine state: `intentPlaying`, `currentType`+`currentCorsOk`, `reconnectAttempts`, `reconnectTimer`, `inTransition` (suppresses cleanup-pause race). Also set `export const maxDuration = 300` on both proxy routes to extend Vercel function lifetime where plan permits | `src/lib/audio.ts`, `src/app/api/stream/route.ts`, `src/app/api/hls/route.ts` |
| M11 | Dual-slot pre-warm + instant swap. Audio engine now keeps two CORS-clean `<audio>` elements wired in parallel through the AudioContext graph (each with its own `MediaElementAudioSourceNode` + per-slot `GainNode`). 270s after the active slot's `playing` event, the inactive slot silently buffers the same proxy URL. On `ended`/`pause`/`error` the engine 50ms-crossfades between slot mixes — no audible gap. Falls back to M10 reconnect when pre-warm isn't ready (HLS streams, transient pre-warm failures). Removed the dead `nocorsEl` path; `getRms()` no longer special-cases active path | `src/lib/audio.ts` |
| M12 | Affordance polish + Suggestion Box. Power button rebuilt with brass bezel + IEC ⏻ glyph; "New Band" plaque switched from brass-on-brass to dark walnut + "+" SVG glyph for readability; "?" button bumped 28→40px with etched ring; About overlay sections expanded 7→12 to cover every interaction in old-timey announcer voice. New Suggestion Box overlay (tabbed: "Suggest a Station" / "Other Suggestion") writes to a new Supabase `public.suggestions` table with insert-only RLS — anon key can write but never read. (Later redesigned as a brass mail-slot in the upper-left cabinet corner: face plate + four corner screws + textured recessed slot + engraved "SUGGESTION BOX" label) | `src/components/Lamps.tsx`, `PresetBar.tsx`, `Console.tsx`, `AboutOverlay.tsx`, `SuggestionBoxOverlay.tsx`, `src/lib/store.ts`, `supabase/schema.sql` |
| M13 | Doze, Tone, Scan, Station Card — four user-facing features in one pass. Audio graph extended: `slot mixes → analyser → bassFilter (lowshelf 200Hz) → trebleFilter (highshelf 4kHz) → masterGain → dozeGain → destination`. Analyser stays *above* EQ so VU shows source signal; dozeGain is its own node *after* masterGain so volume keeps working through a fade. New "service controls" strip below the speaker grille: `[DozePlaque] [TonePanel] [ScanButton]`. Doze cycles OFF→15→30→60→90 min, MM:SS countdown, fades the last 30s. Tone is two compact 92px brass knobs (-12..+12 dB, ±0.75 dB snap-to-zero detent, double-click resets, dB readout). Scan picks a random station from any band every 12s, pulsing lamp; manual tune cancels. Station Detail Card is a modal in the AboutOverlay visual family, opened by clicking the dial caption pill — logo, name, now-playing (re-fetched 30s), country/language/bitrate/format/tags, homepage link, copy-to-clipboard stream URL | `src/lib/audio.ts`, `src/lib/storage.ts`, `src/lib/store.ts`, `src/components/TonePanel.tsx`, `DozePlaque.tsx`, `ScanButton.tsx`, `StationDetailCard.tsx`, `DialWindow.tsx`, `Console.tsx`, `AboutOverlay.tsx` |
| M14 | Deploy + Suggestion Box polish + email pipeline. Idempotent `restoreEmptySeedBands` action in `store.ts` re-adds memberships for any seed-default group that's been emptied (recovered "Around the World" + "Exploratorium" after a corruption episode); runs in `StoreHydrator` after seed/pull and on uid change. Suggestion Box: Contact Email field finally gets the cream `inputStyle` like every other input; new brass magnifying-glass beside Station Name opens an inline directory-search subview that hits `/api/stations` with debounced query — selecting a result autopopulates name + URL + a `country · bitrate · codec · HLS · Tags · homepage` blurb in the notes. ESC backs out of search before closing the overlay. Email-on-suggestion pipeline live: Database Webhook on `suggestions` INSERT → Supabase Edge Function `notify-suggestion` (Deno) → Resend HTTP API → David's inbox. Edge Function lives at `supabase/functions/notify-suggestion/index.ts`; tsconfig + .eslintignore exclude that path so the Next build doesn't choke on Deno globals. Three function secrets (`RESEND_API_KEY`, `NOTIFY_TO`, `NOTIFY_FROM`); `onboarding@resend.dev` works as the test sender without domain verification | `src/lib/store.ts`, `src/components/StoreHydrator.tsx`, `src/components/SuggestionBoxOverlay.tsx`, `supabase/functions/notify-suggestion/index.ts`, `tsconfig.json`, `.eslintignore`, `DEPLOY.md` |
| M15 | Cabinet layout rearrangement (post-launch polish). Top row of the cabinet now reads **POWER · SUGGESTION BOX · ?** — POWER button absolute-positioned at upper-left (`top-2.5 left-3 sm:top-3.5 sm:left-4`) mirroring the "?" on the upper-right (`top-2.5 right-3 sm:top-3.5 sm:right-4`); SUGGESTION BOX centered horizontally between them at the same top offset (`left-1/2 -translate-x-1/2`). Both upper cabinet brass-screws (`top-3 left-3` and `top-3 right-3`) removed to clear room for the buttons; bottom-left + bottom-right screws preserved. Maker's plate ("Your Internet Radio Dial") gained `mt-12 sm:mt-14` so it sits below the new top row instead of colliding with the centered SUGGESTION BOX. ON AIR lamp moved into the center grid column with `flex flex-col` + `flex-1` spacer below `<DialWindow />`, so the row stretches to match the taller volume column on the left and the lamp vertically centers in the empty space between dial face and band ribbon. Model No. plate ("Model No. 1 · Est. 2026") moved out of the footer and into the dial face under "A Fruchtomania Production", recolored from `text-brass-300` to `text-ink/80` for the cream background. Old footer (Power · Model No. · OnAir) deleted entirely. AboutOverlay copy updated: "The Power Switch" now says upper-left, "The Suggestion Box" describes the centered top position, and a new "The On-Air Lamp" section was added between Volume Knob and Bands | `src/components/Console.tsx`, `src/components/DialWindow.tsx`, `src/components/AboutOverlay.tsx` |
| M16 | Cabinet vertical spacing polish. Maker's plate margins tightened (`mt-12 sm:mt-14 mb-3 sm:mb-5` → `mt-10 sm:mt-11 mb-2 sm:mb-3`). Left column VU↔Volume gap tightened (`gap-5` → `gap-3`). VolumeKnob's outer wrapper lost its `gap-2` and the "VOLUME" text label gained `-mt-3` so it rides into the empty visual space at the bottom of the 160px knob box, sitting much closer to the OFF/MAX ring. PresetBar wrapper kept at `mt-6 sm:mt-7` after iteration (one step beyond the M15 baseline) — the bands bar needed a touch more breathing room than the initial pass left it with. ToneKnob's outer box shrunk from 92×92 to 92×80 — the bottom 12px was empty visual space (last tick at y≈71); brass ring + ticks recompose around the new center y=40 since they're anchored via `top:50%, left:50%`. Service-controls strip switched from `items-center` to `items-start` so DOZE and DRIFT button tops align with the BASS/TREBLE knob tops (rather than vertically centering against the taller TonePanel) | `src/components/Console.tsx`, `src/components/VolumeKnob.tsx`, `src/components/TonePanel.tsx` |
| M17 | Reduced-motion caption + font-swap re-measure. Diagnosed the "whip-fast caption scroll on the work machine" as `prefers-reduced-motion: reduce` colliding with the marquee's `infinite` keyframe — the global reduced-motion rule's `animation-duration: 0.01ms !important` made it a ~100 kHz strobe rather than a stationary string. ScrollingCaption now detects reduced-motion via `window.matchMedia` (with a `change` listener so mid-session toggles work) and swaps the marquee for a drag-to-pan single-copy view when the preference is on AND the text overflows. Drag mechanics reuse the dial-strip pattern: `setPointerCapture` on pointerdown, clamped translateX during pointermove (`[visible - textWidth, 0]`), `suppressClickRef` for one frame on pointerup if movement exceeded the 5px `DRAG_THRESHOLD` — so a real drag doesn't also fire the StationDetailCard open. Button overlay's `pointerEvents` flips to `none` mid-drag so it doesn't steal pointer events from the parent; cursor flips between `grab` / `grabbing`; `touchAction: pan-y` preserves vertical page scroll on touch. Pan resets on station change. Defensive secondary fix: `document.fonts?.ready?.then(check)` + observing the measurer span (not just the outer pill) so a late web-font swap re-triggers duration/maxPan recalc. Also tightened the overflow check from `w > c + 1` (clientWidth incl. padding) to `w > visible + 1` where `visible = clientWidth - 32` (true content area excluding `px-4`). Home-machine behavior unchanged — marquee path is unmodified | `src/components/DialWindow.tsx` |
| M17.1 | Suggestion Box RLS hotfix. Friend reported "new row violates row-level security policy for table suggestions" on submit. Diagnosed as the row's `user_id` not matching `auth.uid()` — most likely a stale local session vs missing JWT. Fix: SuggestionBoxOverlay now always sends `user_id: null` on the row regardless of the client's auth state. The RLS policy's first branch (`user_id is null`) always passes, so submission is robust to any session weirdness (expired JWT, iOS Safari ITP, anon-signin disabled, etc). Also: dropped the `user` store subscription (no longer needed) and replaced raw `error.message` exposure with a friendly "Couldn't deliver your card" — real error still logs to console for diagnosis. Production policy was unchanged; the hotfix is purely client-side | `src/components/SuggestionBoxOverlay.tsx` |
| M18 | NOW PLAYING tap-to-identify song-ID lozenge. Brass plaque (final: 260/320px wide, padding `6px 20px 10px`) centered directly under the dial face. Tap captures ~8s of the live audio output via a new `MediaStreamAudioDestinationNode` (`captureDest`) connected in parallel to the analyser output (so capture is independent of user volume / Doze fade), POSTs it as multipart form-data to a new `POST /api/song-id` route. Route checks new Supabase `song_id_cache` table (60s TTL, station-level, anon-permissive RLS) before forwarding to AudD via FormData. State machine: idle / listening / success / unknown / error — actual error message surfaced in the cream window for diagnosability rather than hidden behind generic "tap to retry". Reduced-motion gate on the listening dot's pulse animation (M17 lesson). Lozenge dims to 0.55 opacity and shows "turn on the radio to identify a song" when `playback.isPlaying` is false. ON AIR lamp re-aligned to `items-end` of its spacer with `mt-6 sm:mt-8 pb-1 sm:pb-2` so the NOW PLAYING plate gets breathing room. New env var `AUDD_API_TOKEN` (server-side, no NEXT_PUBLIC prefix). Cache table reads/writes with the existing anon key. New schema block at the bottom of `supabase/schema.sql` (idempotent). **Cream window has three render branches:** (a) `dragMode` (reduced-motion + overflow) — single copy at `translateX(currentPan)` with click-and-drag panning, mirrors the dial-caption pattern; (b) `marquee` (motion-allowed + overflow) — two copies + global `@keyframes marquee`, ~50px/sec, 8s min; (c) static — single copy with truncate. Drag handlers attach to the cream window (not the whole button) so a tap-without-drag inside still triggers song-ID; the brass-area-only tap path is unaffected. One slice-confusion bug caught during iteration — the lozenge initially read `s.isPlaying` (top-level, undefined) instead of `s.playback.isPlaying` (nested), gating the lozenge off forever; saved as a reference memory | `supabase/schema.sql`, `src/app/api/song-id/route.ts`, `src/lib/audio.ts`, `src/components/NowPlayingLozenge.tsx`, `src/components/Console.tsx` |
| M19 | Between-station tuning static. New `staticGain`+`staticBuffer`+`staticSource` summed into the analyser input — pink noise (Kellet IIR cascade) + 1-pole HP (~350Hz) + sparse crackle (~22Hz, ±0.4 amplitude) in a 4s looped buffer. Routes through the same EQ/master/Doze chain as audio so static obeys volume + tone + Doze fade and drives VU. Envelope: 150ms fade-in + 400ms plateau + 300ms lock-in (total floor ~0.85s, peak gain 0.33). Slow loads use a 50ms overrun snap so total ≈ load time. New `'tuning'` status on `AudioStatus` (lamp steady amber + label "Tuning…", caption flips to "Tuning…"). New public engine API: `tune()` replaces `play()` as the entry from `store.play()`; `beginDragTuning()` raises static on dial-drum drag past threshold (power-off-guarded — early returns when `intentPlaying` is false); `endDragTuning()` closes out a same-station drag-release. M10 reconnect now bridges its silent gap with the same envelope (`scheduleReconnect` raises static, full 300ms lock-in via `tuneAllowOverrunSnap=false`); status stays `'buffering'` so the lamp pulses and the dial caption keeps the station name. M11 pre-warm swap stays silent. **Five bugs caught + fixed during iteration:** (1) `ensureContext` early-returns without slot DOM elements → `tune()` was silently falling through to plain `play()` on first power-on; fix: call `ensureDom()` at the top of `tune()` and `beginDragTuning()` mirroring `play()`. (2) `AudioBufferSourceNode.start(0)` on a still-suspended context can leave the source not producing samples; fix: defer `start()` to AFTER `ctx.resume()`, gated by `staticSourceStarted` flag. (3) `beginDragTuning` was setting `intentPlaying=true` itself → power-off drag triggered orphan static with no lock-in path; fix: bail when `!intentPlaying`, no longer mutates the flag. (4) Slow loads were always paying 300ms lock-in on top of load time; fix: `'playing'` event handler computes overrun + uses 50ms snap when `tuneAllowOverrunSnap` is true. (5) Same-station click → 2.25s static for a no-op tune; fix: `setCurrentStation` early-returns when id is unchanged AND playing AND status isn't `'tuning'` (the exception lets a mid-drag same-station release still complete lock-in) | `src/lib/audio.ts`, `src/lib/store.ts`, `src/components/DialWindow.tsx`, `src/components/Lamps.tsx` |
| M20 | iOS graceful degradation. Investigation flow: (1) wired a parallel captureStream diagnostic on slot[0] via the existing NowPlayingLozenge tap to confirm/refute whether `el.captureStream() → createMediaStreamSource` would carry signal on iOS. Desktop Chrome read `pk=0.87 n=87 cs=0.88(live)` — captureStream works on desktop. iPhone read `pk=0.00 n=94 cs=N/A(captureStream not available on HTMLAudioElement)` — confirmed unsupported on iOS. (2) Tested `<video>` element instead of `<audio>` (WebKit historically exposed captureStream on HTMLVideoElement first); iPhone read `cs=N/A(captureStream not available on <video>)` — also unsupported. (3) Considered WebRTC loopback (needs an upstream MediaStream that doesn't exist on iOS without captureStream), and full custom JS streaming-decoder path (1–2 week rewrite). Settled on graceful degradation. (4) First-pass iOS shipped a `setVolume` branch that wrote to `el.volume` thinking it would work — David's iPhone test caught that Apple silently ignores programmatic `el.volume` writes on `<audio>` elements as platform policy (only hardware buttons control output). Reverted that, greyed the volume knob too. Also caught: the `title` attribute does nothing on iOS touch (no long-press tooltip surface) — added persistent inline captions instead. New `src/lib/isIos.ts` UA-based detector (handles iPadOS-13+ Mac-pretender via `navigator.maxTouchPoints > 1`), used in 5 components: (a) `VolumeKnob.tsx` greys at 0.4 opacity + pointer-events overlay, "iPhone: use hardware buttons" caption beneath the VOLUME label. (b) `TonePanel.tsx` greys both knobs the same way, "iPhone: not available" caption beneath the row. (c) `VUMeter.tsx` overlays a grey "VU meter / not available / on iPhone" cream-tint over the dial face, suppresses the existing meter-unavailable badge in that mode. (d) `NowPlayingLozenge.tsx` fixed cream-window text "Song identification not available on iPhone", tap disabled, plate dimmed to 0.55. (e) `audio.ts` is back to its pre-M20 volume code path — no iOS-specific writes (they're inert anyway). iOS detection uses the `useState(false) + useEffect(() => setIosMode(isIos()))` hydration-safe pattern across all four UI components. M19 tuning static and audio playback itself are unchanged — static is synthesized in WebAudio direct to destination; audio plays via the element's platform-direct path. DOZE timer still fires (hard cut at the end instead of the 30s fade — the dozeGain ramp is silent on iOS). The two M20-diagnostic commits + the `<video>` element experiment were rolled back in this commit; only the iOS-detection wiring remains | `src/lib/isIos.ts` (new), `src/lib/audio.ts`, `src/components/VolumeKnob.tsx`, `src/components/TonePanel.tsx`, `src/components/VUMeter.tsx`, `src/components/NowPlayingLozenge.tsx` |

---

## Architecture notes worth preserving

### Dual-slot audio engine (`src/lib/audio.ts`) — M11

- Two CORS-clean `<audio>` elements (`slots[0].el`, `slots[1].el`), both `crossOrigin="anonymous"`. Each is permanently wired through its own `MediaElementAudioSourceNode → GainNode (mix)` at context init — `createMediaElementSource` can only be called once per element, so lazy wiring would lock us out.
- Audio graph: `slot.el → mes → mix → analyser → masterGain → destination`. Both slot mixes feed the same shared analyser/master chain. Per-slot mix gain is `1` for the active slot and `0` for the other. masterGain holds the user's volume independently, so the VU meter and volume control are unaffected by swaps.
- `activeIdx: 0 | 1` tracks which slot is audible. Status updates and pre-warm scheduling key off this — events from the inactive slot are filtered out (except `playing`, which records `slot.startedAt`).
- **Pre-warm:** 270s after the active slot's `playing` event (Vercel cap is 300s; 30s safety margin), the inactive slot is pointed at the same proxy URL with mix gain `0` and `.play()` is called. Silently buffers ahead of the cut.
- **Swap:** when active fires `ended`/`pause`/`error`, `tryFailoverOrReconnect` checks `canSwap()` (pre-warm active, `readyState ≥ 2`, not paused, not HLS). If yes: 50ms `linearRampToValueAtTime` crossfade between mix gains, flip `activeIdx`, tear down old slot, reschedule pre-warm based on the new active's `startedAt`. If no: fall back to M10 `scheduleReconnect`.
- **HLS exclusion:** `currentIsHls` skips `schedulePrewarm`. HLS streams use M10 reconnect on drop. Pre-warming a parallel `hls.js` instance pointed at the same manifest is more invasive than this milestone wanted to land.
- **Three guards interact subtly** — `intentPlaying`, `inTransition`, and `prewarmActive`. Any path that synchronously calls `el.pause()` or `el.removeAttribute("src"); el.load()` needs the `inTransition` flag set, or the resulting pause/emptied event will be misread as an unexpected drop and trigger a spurious reconnect/swap. See comments in `audio.ts` lines 70–95.

### Audio extensions (`src/lib/audio.ts`) — M13

The graph grew two `BiquadFilterNode`s and a per-output `dozeGain`:

```
slot.el → mes → mix ─┐
                     ├→ analyser → bassFilter (lowshelf 200Hz) → trebleFilter (highshelf 4kHz) → masterGain → dozeGain → destination
slot.el → mes → mix ─┘
```

- **Analyser placement** — kept *above* the EQ filters on purpose. The VU meter reflects the source signal, not what the user's EQ is doing. If you move it post-EQ, cranking treble visually pegs the meter without the music actually getting louder.
- **dozeGain after masterGain** — a separate output-stage gain so a Doze fade-out (`linearRampToValueAtTime` over the final 30s) doesn't fight the user's volume knob. They can still ride volume during the fade without breaking the envelope.
- **Public API:** `setBass(db)` / `setTreble(db)` / `getBass()` / `getTreble()` for tone; `startDoze(totalSeconds)` / `cancelDoze()` / `getDozeRemainingMs()` for the sleep timer. The tone setters update the BiquadFilterNode `.gain.value` directly. `startDoze` sets up a `setTimeout` that fires the fade ramp 30s before the end and a second `setTimeout` that calls `stop()` at the end.
- **Persistence quirk** — bass/treble write to IndexedDB only (the existing UserData blob gained two optional fields without a version bump; missing values default to 0). They do *not* sync to Supabase yet — schema would need new columns. Acceptable because EQ is a per-device, per-room preference anyway.

### Scan loop (`src/lib/store.ts`) — M13

- Module-level `scanTimer: ReturnType<typeof setInterval> | null`, with `startScanInterval()` / `stopScanInterval()` helpers — a singleton at module scope so React renders can't multiply the timer.
- `pickRandomStation` filters to stations that are members of *some* group (skips orphaned imports) and picks across all bands, not just the active one — the whole point is serendipity.
- Period: 12s.
- The drift uses `useRadioStore.setState` directly + `engine.play()` to bypass `setCurrentStation`'s "manual tune cancels scan" safeguard — without that, the first auto-drift would cancel itself.
- Manual tune (any call to `setCurrentStation`) cancels scan. `pause()` cancels both scan and Doze.

### `corsOk` flag on `Station` (`src/data/seed.ts`)

- Default `true` (assume origin is CORS-clean).
- Set to `false` only when the origin refuses `crossOrigin="anonymous"` requests.
- Engine logic: `useProxy = !corsOk`; then HLS → `/api/hls`, else → `/api/stream`.

### `CURRENT_VERSION` in `src/lib/storage.ts`

Invalidates the user's IndexedDB cache on mismatch → forces re-seed. Bump whenever `seed.ts` shape changes so stale cached data doesn't shadow the new seed. Currently `10`.

### `/api/hls` manifest rewriting

- Rewrites bare segment URLs AND every `URI="..."` attribute on `#EXT-X-KEY`, `#EXT-X-MEDIA`, `#EXT-X-MAP`, `#EXT-X-I-FRAME-STREAM-INF`.
- Uses `upstream.url` (post-redirect) as the base for resolving relative paths.
- Binary-passthroughs segment requests; rewrites only when content-type looks like `mpegurl` or the URL ends with `.m3u8`.

### M3 library mutations — dense-position invariant

Every mutation in `src/lib/store.ts` that touches `groups` or `memberships` renumbers the affected group's `position` field to a dense `0..N-1` sequence. This makes `moveGroup` / `moveStationInGroup` a simple array swap + renumber and means UI sort-by-position is always stable.

- **User-added stations default to `corsOk: false`** — radio-browser doesn't expose CORS cleanliness, so forcing the proxy path is the always-works choice. Tiny latency hit, zero breakage.
- **Url-based dedup on add** — if an added radio-browser station shares its `streamUrl` with an existing station (e.g. a seed entry for Jazz24), the existing record is reused rather than creating a parallel entry with a different id.
- **Deleting the active group** auto-selects the first remaining group and tunes its first station (or nulls out if empty). **Last band cannot be deleted.**
- **Removing the currently-playing station** auto-advances to the next station in the band.

---

## Gotchas learned the hard way

- **Dead URLs masquerade as CORS problems.** When a station goes silent, check the server log first. `[/api/stream] upstream status 404` means the seed URL is stale, not that CORS is broken.
- **`radio-browser.info` is the source of truth for stream URLs.** API requires a `User-Agent` header or returns `[]`. Example: `curl -s -H "User-Agent: YIRD/1.0" "https://de1.api.radio-browser.info/json/stations/byname/<name>"`.
- **HLS needs same-origin for manifest AND segments** — not just the manifest. Segment URLs are embedded in the manifest; that's why M2c rewrites them.
- **BBC's master-playlist redirectors return stale pool numbers** (pool_904 → 410 Gone). Prefer the MP3 Icecast URL (`stream.live.vc.bbcmedia.co.uk/bbc_world_service`).
- **Monocle 24 and DW English moved behind paywalls / HLS-only.** `ABC News Radio (AU)` and `WNYC` are the working replacements.
- **VU meter SSR hydration warning** — `tickGeom` floating-point math produces tiny server/client mismatches. Known, not yet fixed. Fix by rounding tick coordinates to fixed precision or marking the SVG client-only.

---

## M4 — shipped

Supabase auth + cloud sync complete. Design notes:

- **Anonymous-by-default.** Every visitor gets a real `auth.uid()` on first load via `signInAnonymously`. No signup gate, and the uid persists in localStorage so a returning visitor keeps the same identity.
- **Single-user model, no merge logic.** `updateUser({ email })` on an anon user preserves the uid — all cloud rows keyed by `user_id` transfer automatically when they upgrade. No row-level merge code needed.
- **Write-through dual-sync.** `schedulePersist` writes to IndexedDB always; if `user` is set, it also pushes to cloud via `syncToCloud` (wipe-and-replace on each persist — good enough for a single-user library of this size).
- **Pull-on-load.** `StoreHydrator` sequence: hydrate IndexedDB → ensureAnonSession → pullFromCloud. Cloud wins on conflict. If cloud is empty, local seed is pushed up.
- **Auth-change subscription.** Email confirmation link flips the store user from anon to permanent in-place (same uid, now has email, `isAnonymous:false`) so the AccountDrawer updates without a page reload. Sign-out immediately mints a fresh anon session.
- **StrictMode lock.** `ensureAnonSession` is guarded by an in-flight promise so React 18 double-invoked effects don't create two anon users.
- **Supabase quirk.** Email upgrade uses `updateUser({ email })`, which fires the **"Change email address"** email template (not "Confirm sign up"). Since anon users have no prior email, the default template's "from [blank]" wording is odd — edit that template in the dashboard to read like a signup confirmation. See `supabase/SETUP.md`.

## M5 — shipped

Pre-deploy UX polish. Closed the entire design-dependent backlog in one pass so the first public version looks finished.

- **Dial-station overflow (drum-style ticker).** When a group has more stations than fit on the arc, the dial swaps to a horizontal drum/ticker strip: the red needle stays fixed dead-center (`left:50%, width:1px`, `#e34848 → #a82222` gradient), and station markers slide under it. Supports drag-to-tune AND click-to-jump.
- **PresetBar overflow.** Edge mask-fade + mouse drag-scroll + brass arrow-steppers that auto-hide at ends. Uses document-level pointer listeners instead of `setPointerCapture` so PresetButton long-press timers still cancel. PresetButton has its own 5px movement threshold that cancels long-press when the user is actually scrolling. The old "+" preset was replaced with a centered **"New Band"** brass plaque below the bar. Genre band removed from top of dial; **"A FRUCHTOMANIA PRODUCTION"** centered tagline there now.
- **About / How-to overlay.** `AboutOverlay.tsx`, triggered by a brass "?" at `absolute top-2 right-5 sm:top-3 sm:right-6`. Ivory-on-walnut service-manual body styling; 7 short sections in a warm 1940s-announcer tone. Store flag: `ui.aboutOpen` + `setAboutOpen`.
- **Vertical brass RADIO / BANDS labels.** Flank the dark PresetBar on the walnut, outside the cabinet pill. `<VerticalBrassLabel>` helper stacks upright uppercase letters at 11px / lineHeight 1.35 / letterSpacing 0.1em. Centered via `items-center` on the outer row so labels can overhang the bar naturally.
- **Favicon.** `src/app/icon.svg` — Next.js App Router auto-serves it. Cathedral-radio silhouette: arched walnut cabinet, brass-bezel amber dial with red needle at top, three brass grille bars. Silhouette-first so it reads at 16×16.

## M6 — shipped

Cross-device sign-in via magic link. Closes the gap from M4 (which assumed one device per account).

- **`signInWithEmail(email)` in `client.ts`** — wraps `signInWithOtp({ email, options: { shouldCreateUser:false, emailRedirectTo: window.location.origin } })`. `shouldCreateUser:false` means an unknown email errors clearly instead of silently creating a fresh user. `emailRedirectTo: window.location.origin` makes the link land back on whichever host (localhost or prod) sent it, so dev and prod both work without code changes — both must be in Supabase → URL Configuration → Redirect URLs.
- **AccountDrawer mode toggle** — anon state branches on a `mode: "new" | "existing"` flag. Same email field, different submit handler; toggle link at the bottom of the form (`Already have an account? Sign in instead →`). Sign-in copy includes a one-line warning: "Signing in will replace this device's guest library with your synced library."
- **StoreHydrator uid-change detection** — `knownUidRef` tracks the currently active uid. Auth-change subscriber compares prev vs new uid; on mismatch (only happens for true cross-device sign-in, NOT for the in-place anon→permanent upgrade), it runs `pullFromCloud + applyCloudSnapshot` to overwrite this device's local state with the user's synced library. Orphaned anon-uid rows in cloud are intentionally left behind.
- **Supabase dashboard step (must do once)** — enable the "Magic Link" email template in Authentication → Email Templates. Edit if needed to match the brand voice. Without this enabled, `signInWithOtp` will return an error.

### Suggestion-box email pipeline (M14)

End-to-end flow: browser submits → Supabase `public.suggestions` row inserted → Database Webhook `notify-suggestion-on-insert` POSTs to Edge Function `notify-suggestion` → function reads payload, composes plain-text email, calls Resend API → email arrives at `NOTIFY_TO`.

- **Edge Function** — `supabase/functions/notify-suggestion/index.ts` runs in Deno. Reads `RESEND_API_KEY`, `NOTIFY_TO`, `NOTIFY_FROM` from `Deno.env`. Subject + body branch on `kind` ("station" vs "other"). 405 if not POST, 500 if secrets missing, 502 if Resend rejects (with the upstream status + body included so Supabase webhook logs are useful).
- **Build excludes** — `tsconfig.json` excludes `supabase/functions/**`, and `.eslintignore` does the same. Without these, `next build` errors on `Cannot find name 'Deno'`.
- **Webhook** — Supabase Database Webhook named `notify-suggestion-on-insert`, on `public.suggestions`, INSERT only, type "Supabase Edge Functions", target `notify-suggestion`, 1000ms timeout. Auth header is auto-injected.
- **Sender quirk** — using `onboarding@resend.dev` as `NOTIFY_FROM` requires no domain setup but mail can land in spam on first send. Whitelist or verify `dawgranch.org` in Resend later if it becomes a problem.

## M20 — shipped (graceful iOS degradation)

See the M20 row in the milestone table for the full story (investigation → captureStream-on-`<audio>` ruled out → captureStream-on-`<video>` also ruled out → WebRTC loopback ruled out → graceful degradation chosen). Test plan to run after pushing:

- **Desktop (Chrome / Firefox / Safari):** verify nothing regressed. Volume knob attenuates, Bass/Treble shape audio, Doze fade audible over the last 30s of a timer, VU meter wiggles to content, NOW PLAYING identifies a song. M11 pre-warm swap still seamless.
- **iPhone (Safari + Chrome):** Volume knob is greyed at 0.4 opacity with persistent caption "iPhone: use hardware buttons" — Apple silently ignores programmatic `el.volume` writes on `<audio>` elements. Bass/Treble greyed the same way with "iPhone: not available" caption (the `title` tooltip is invisible on iOS touch — long-press doesn't surface it). Doze timer fires the hard cut at the scheduled end (no audible fade — this is intentional). VU meter shows the "VU meter / not available / on iPhone" overlay. NOW PLAYING lozenge stays visible but cream window says "Song identification not available on iPhone" and tap is a no-op. M19 tuning static still plays on every tune (must not regress — synthesized in WebAudio, doesn't depend on MES). Audio playback itself unchanged.
- **iPad / iPadOS:** same as iPhone. The detector specifically handles the iPadOS-13+ "I'm a Mac, no really" case via `navigator.maxTouchPoints`.

---

## Deferred

- VU meter SSR hydration warning — cosmetic, low priority. Fix by rounding tick coordinates to fixed precision or marking the SVG client-only.
- Settings menu — `ui.menuOpen` / `setMenuOpen` are still in the store but no consumer. The PresetBar hamburger now opens the station list (M3.4); a new icon could be added for a future settings overlay.
- ESLint warning: `VUMeter.tsx:113` missing `startDeg` dep in `useEffect`. Non-blocking on Vercel; fix by adding the dep or disabling the rule inline.
- Orphan-anon-row cleanup — when a user signs in on a fresh device, the local anon uid's cloud rows (just the seed library, pushed by the bootstrap effect) are abandoned. Low priority; could be cleaned up by a Supabase scheduled function that deletes anon-user rows older than N days.

---

## Dev loop

`npm run dev` → `http://localhost:3000`. Hard-reload (Ctrl+Shift+R) after any `CURRENT_VERSION` bump.

Workspace folder name is always "Your Internet Radio Dial". Don't hardcode absolute paths that include session IDs.
